A very incomplete list of proposed libraries
--------------------------------------------

AI
  GA
  GP
  machine learning

algorithms
  partition
  sort
  search
    binary search
    proportional search

binary
  binary string/block
  binary <-> string
  bitmaps 
    bloom filters 
    compression 
    and/or/not/nand/nor/xor/xnor
    contains 0, contains 1, count 1s, count 0s.
    find pattern, count pattern
    reverse
  boolean/bit
    and/or/not/nand/nor/xor/xnor

caching
  buffers
  circular buffers

concurrency
  condition variables
  mutexes
    debug mutexes
    re-entrant mutexes
    spin mutexes
  parallelisation functions
  priority queue
  processes
  threading
    threads
    thread group
  vectoring
  
compression
  archives
    tar
    zip
  (bitmap compression)
  gzip
  bz2

cryptography
  MAC
  certificates
  encryption
  digital signatures, group signatures
  cryptographic hashes
  merkle trees
  "password" hashes
    bcrypt
  random number gneeration
  zero knowlege algorithms

data structures
  array/vector
  "combination structures" (made up of combination of primitive structures).
  forests
  graph
    DAG
  grid
  hash tables
  "large data stuctures" (i.e. too big to fit in memory).
  lock free data structures
  list
    skip lists
  maps & bimaps
  pair/tuple
  queues
  set
  stacks/deques
  tree
   AVL tree
   B+tree
   B-tree
   Red-block tree
   prefix tree/trie

database
  statement
  prepared statement
  table
    csv
  sql

date & time
  date
  date with time
  date parsing
  time
  time zone
  timer

dimension
  metric, imperial measurements & conversions

error handling & logging
  asserts & debugging
  error type(s) (error code + error message stack)

file & filesystem & I/O
  file locks
  file path
  directory traversal
  buffered & umbuffered file access
  virtual file system

formats & protocols
  bson
  json
  xml
  yaml

formatting
  layout
    LaTeX

graphics
  colour
  fonts
  image formats
    png etc
    large format (i.e. too big to fit in memory).
  geometry
    curves/splines
    polygons
    platonic solids
  (layout)
  2D images
    graphing
    geometry    
    animation
  3D images
    geometry  
    graphing
  vector
    svg
  video
    
language
  parsing
  text to speech
  speech recognition?

locale
  currency
  locale
  i8ln
    number/currency parsing

maths
  arbitrary percision
  big number
  complex numbers, quaternions, octonions.
  factoring & prime number tests
  FFT/IFFT
  geometry
    2D, 3D, n-dimensional?
  linear programming
  logic, symbols & fractions
    integration/differentiation
    solver
  markov chains
  matrices & tensors
    sparse matrix
  modulo arithmetic
  non-standard numeric type sizes (e.g. array of 4 bits, a 128 bit float etc).
  non-euclidean coordinate systems
    polar coordinates
  polynomials
    chebyshev
  pseudo-random number generation
  numeric interval
     to/from inclusive,exclusive
  rolling hashes
  statistics
    distributions
    linear regression
    histogram
  trigonometry
  vector/array maths
  
memory management
  heap management
  shared memory APIs

network
  ipv4/v6
    tcp
    udp
    sctp
  sockets
  client & servers
    multiplexing

parser
  tokeniser
  parser
  (math symbolic parser)

query (linq)

reflection

regular expressions

science
  chemistry
    period table / molecules?
  earth science / geoscience
    coordinate systems
      geodetic datum
    long/lat type
  physics
    fundamental physics constants
    physics equations (e.g. pressure etc).
    finite state modelling engine?
    basic physics engine?
  life science / biology
    dna sequences

scripting integration

sequences / iterators / ranges

serialisation
  base64
  b+ tree
  checksum
    crc
  endian conversion
  storage/transmission protocol

sound
  (fft/ifft)
  signal processing?
  
string
  canonical strings / flyweight pattern
  fixed length strings
  string builders
  string manpilatuion
  string formatting
  string search algorithms

test harness

UI
  (layout)  
  terminal
    argument parsing
    command history
    (layout)

versioning & Identification
   semantic version number
   UUID

web
  css
  html
  http/https
  (layout)
